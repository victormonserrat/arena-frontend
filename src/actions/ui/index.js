import * as constants from './constants';

export const showDrawer = () => ({type: constants.SHOW_DRAWER});
export const hideDrawer = () => ({type: constants.HIDE_DRAWER});
export const showLogin = () => ({type: constants.SHOW_LOGIN });
export const hideLogin = () => ({type: constants.HIDE_LOGIN});
